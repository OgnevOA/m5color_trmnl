# Python
__pycache__/
*.py[cod]
.venv/
venv/

# App runtime data
/data/
mock_display/
.client_state.json

# Secrets / local config
.env

# Firmware local config & secrets (WiFi creds, device token, static IP).
# secrets.h.example is the committed template.
secrets.h

# OS / editor
.DS_Store

# Planning artifact (not part of the deliverable repo)
plan.md